# Web Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
pydantic==2.5.3
pydantic-settings==2.1.0

# AWS & Cloud Storage
boto3==1.34.20
s3fs==2024.2.0

# Data Processing
pyarrow==15.0.0
pandas==2.2.0

# Apache Spark (local mode - Phase 6&7)
# NOTE: Connects to Spark cluster running in Docker (see docker-compose.yml)
pyspark==3.5.1  # Matches Docker apache/spark:3.5.1 image

# Delta Lake for Spark (Phase 6)
delta-spark==3.2.0  # Compatible with Spark 3.5.x

# Delta Lake Python library (Phase 5 metadata reading)
deltalake==0.15.0

# Configuration & Environment
python-dotenv==1.0.0

# Logging & Monitoring
structlog==24.1.0
python-json-logger==2.0.7

# Testing
pytest==8.0.0
pytest-asyncio==0.23.3
httpx==0.26.0

# Code Quality
black==24.1.0
ruff==0.1.14
